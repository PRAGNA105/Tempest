"""clean_app — parallel control codebase for localguard.

Same shape as demo_app, but every endpoint points at localhost or
staging.internal with no live/upstream references anywhere. Used for
the "zero false positives" demo.
"""
